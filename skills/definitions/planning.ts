import type { SkillDefinition } from "@/skills/types";

export const planningSkill: SkillDefinition = { id: "planning", label: "Planning", description: "Reusable planning behavior", instructions: [], contextRequirements: ["project"], enabled: false };
