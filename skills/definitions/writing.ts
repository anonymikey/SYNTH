import type { SkillDefinition } from "@/skills/types";

export const writingSkill: SkillDefinition = { id: "writing", label: "Writing", description: "Reusable writing behavior", instructions: [], contextRequirements: [], enabled: false };
