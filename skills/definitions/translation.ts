import type { SkillDefinition } from "@/skills/types";

export const translationSkill: SkillDefinition = { id: "translation", label: "Translation", description: "Reusable translation behavior", instructions: [], contextRequirements: [], enabled: false };
