import type { ToolDefinition } from "@/tools/types";

export const searchTool: ToolDefinition = { id: "search", label: "Search", category: "search", requiresApproval: false, enabled: false, inputSchema: {} };
