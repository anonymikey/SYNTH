import type { ToolDefinition } from "@/tools/types";

export const terminalTool: ToolDefinition = { id: "terminal", label: "Terminal", category: "terminal", requiresApproval: true, enabled: false, inputSchema: {} };
